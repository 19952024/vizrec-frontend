# Optional: Use FastAPI backend for SentraCore API instead of file-based storage
# When set, /api/sentra-core/* requests are proxied to this URL
# Leave unset to use built-in Next.js API (file-based storage in data/)
BACKEND_URL=http://localhost:8000
