# Required for database storage: Use FastAPI backend for auth and SentraCore
# When set, /api/auth/* and /api/sentra-core/* are proxied to this URL
# Data is stored in MongoDB when backend is used
# Leave unset for file-based storage (data/users.json, data/sentra-core-configurations.json)
BACKEND_URL=https://vizrec-backend.onrender.com
